# ChillPass Web 期末冲刺助手 — 使用说明

## 简介

ChillPass 是一款 AI 驱动的闯关式备考应用。上传课件后，AI 自动提炼考点、生成闯关课程、智能评阅答题，帮你高效备考。

---

## 快速启动

### 方式一：双击 exe（推荐）

1. 双击 `chillpass.exe`
2. 浏览器自动打开 `http://localhost:5174`
3. 托盘区域出现 ChillPass 图标（右键可"打开网页"或"退出"）
4. 关机前右键托盘 → **退出**，或直接关闭浏览器标签页即可

> 首次启动稍慢（约 3-5 秒），后续启动即时。  
> exe 内含 Node.js 运行时，目标机器**无需安装 Node.js**。

### 方式二：开发模式（热更新调试）

```bash
# 安装依赖（仅首次）
npm install

# 启动开发服务器（改代码自动刷新）
npm run dev
```

访问 `http://localhost:5173`，修改 `src/` 下代码浏览器即时更新。

---

## 构建

```bash
# 生产构建（输出到 dist/）
npm run build

# 重新打包独立 exe（需先 npm run build）
powershell -ExecutionPolicy Bypass -File build-exe.ps1
```

---

## 功能速览

| 功能 | 入口 | 说明 |
|------|------|------|
| **导入课件** | 左侧栏 → 导入课件 | 支持 PDF、PPTX、TXT、Markdown，AI 自动解析 |
| **闯关冲刺** | 左侧栏 → 闯关冲刺 | 按考点闯关，含选择题、填空题、简答题 |
| **Athena AI** | 左侧栏 → Athena | 自由提问、论文代写、知识总结、复习计划等 |
| **错题本** | 左侧栏 → 错题本 | 自动收录错题，可查看解析和笔记 |
| **教师工作台** | 设置 → 教师模式 | AI 出卷、多语言翻译、导出 PDF |
| **主题切换** | 设置页 | 浅色 / 深色 / Vista / Win95 / Codex |
| **语言切换** | 设置页 | 简体中文 / English / Русский / 日本語 / 한국어 |

---

## 数据存储

所有数据存储在 **浏览器本地**（并非服务器）：

| 数据 | 存储位置 |
|------|---------|
| 课程、关卡、学习进度 | localStorage (`chillpass-course-v2`) |
| 聊天记录（Athena） | localStorage (`chillpass-chat`) |
| API 配置、主题、语言 | localStorage (`chillpass-settings`) |
| 错题本 | localStorage (`chillpass-wrong-questions`) |
| 上传的课件文件 | IndexedDB (`chillpass-files`) |

> **清除数据**：设置 → 数据管理 → 清除所有课程数据。  
> 清除后所有本地数据不可恢复。

---

## 配置 DeepSeek API

使用 AI 功能（考点提取、题目生成、AI 评阅、Athena 对话）需要配置 API Key：

1. 进入 **设置 → API 配置**
2. 填入你的 DeepSeek API Key（[deepseek.com](https://platform.deepseek.com/api_keys)）
3. 可选：调整模型（默认 `deepseek-chat`）、温度等参数

---

## 常见问题

**Q: 双击 chillpass.exe 没反应？**  
A: 请确保 `dist/` 文件夹与 `chillpass.exe` 在同一目录。首次启动可能需要等待几秒。

**Q: 双击后又打开了一个新页面？**  
A: 这是正常行为。你已经运行了一个实例，它会自动把浏览器聚焦到已有页面。

**Q: PDF 导出公式显示空白？**  
A: 确保导出时网络正常（KaTeX 字体需要从页面加载）。如果仍空白，请使用 Chromium 内核浏览器（Edge/Chrome）。

**Q: 数据会丢失吗？**  
A: 浏览器清除缓存时可能删除本地数据。建议定期将重要数据导出备份。

**Q: 杀软报毒？**  
A: chillpass.exe 是使用 Node.js SEA 技术打包的独立应用，并非病毒。未做数字签名时部分杀软会有启发式警告，信任即可。

---

## 技术栈

- **前端**: React 18 + TypeScript + Vite 5
- **状态管理**: Zustand（localStorage 持久化）
- **AI 引擎**: DeepSeek API
- **桌面壳**: Node.js SEA（单可执行应用）
- **样式**: CSS Modules + Framer Motion 动画
- **文件解析**: PDF.js / JSZip / Tesseract.js
- **公式渲染**: KaTeX

---

## 项目结构

```
ChillPass/
├── chillpass.exe        # 独立 exe 启动器（双击启动）
├── dist/                # 前端构建产物
├── tray.ps1             # 系统托盘脚本
├── src/                 # 源码目录
│   ├── components/      # 公共组件
│   ├── pages/           # 页面组件
│   ├── services/        # 核心服务（DeepSeek API、文件解析等）
│   ├── stores/          # Zustand 状态管理
│   ├── types/           # TypeScript 类型定义
│   ├── utils/           # 工具函数
│   └── styles/          # 全局样式与主题
├── server.mjs           # 开发服务器
├── vite.config.ts       # Vite 配置
├── package.json         # 依赖管理
└── 使用说明.md           # 本文件
```
