# ChillPass Web 期末冲刺助手 — 使用说明

## 简介

ChillPass 是一款 AI 驱动的闯关式备考应用。上传课件后，AI 自动提炼考点、生成闯关课程、智能评阅答题，帮你高效备考。

v1.2.3 起已从 Electron 迁移为**浏览器版 Web 应用**，通过 Node.js SEA 打包为单文件 exe，启动本地 HTTP 服务器后用浏览器访问。数据 100% 存储在本地，完全离线运行。

---

## 快速启动

### 方式一：安装程序（推荐）

1. 下载 `ChillPass-Setup-0.0.7.exe`（[GitHub Releases](https://github.com/Koipoppy/ChillPass-Web/releases)）
2. 双击安装（无需管理员权限，安装到用户目录）
3. 安装后自动在桌面创建快捷方式；安装时可选：开机自启、安装后立即启动
4. 启动后浏览器自动打开 `http://localhost:5174`
5. 托盘区域出现 ChillPass 图标（右键可"Open in Browser"或"Quit"）

> 开始菜单中有"检查更新"快捷方式，可一键检查并安装新版本。

### 方式二：直接运行 exe（免安装）

1. 确保以下文件在同一目录：
   - `chillpass.exe`
   - `dist/` 文件夹（前端构建产物）
   - `tray.ps1`（系统托盘脚本）
   - `icon.ico`（图标）
   - `updater.ps1`（自动更新脚本）
2. 双击 `chillpass.exe`
3. 浏览器自动打开 `http://localhost:5174`

> 首次启动稍慢（约 3-5 秒），后续启动即时。  
> exe 内含 Node.js 运行时，目标机器**无需安装 Node.js**。

### 方式三：开发模式（热更新调试）

```bash
# 安装依赖（仅首次）
npm install

# 启动开发服务器（改代码自动刷新）
npm run dev
```

访问 `http://localhost:5173`，修改 `src/` 下代码浏览器即时更新。

---

## 构建

```bash
# 1. 生产构建（输出到 dist/）
npm run build

# 2. 打包独立 exe（需 Node.js 22+，支持 SEA）
node installer/build-sea.mjs

# 3. 生成 NSIS 安装程序（需 NSIS 3.x）
makensis installer/installer.nsi
# 或使用一键脚本：
powershell -ExecutionPolicy Bypass -File installer/build-installer.ps1
```

> `build/` 和 `installer/tools/` 为构建中间产物，已在 `.gitignore` 中忽略。

---

## 功能速览

| 功能 | 入口 | 说明 |
|------|------|------|
| **导入课件** | 左侧栏 → 导入课件 | 支持 PDF、PPTX、TXT、Markdown，AI 自动解析 |
| **闯关冲刺** | 左侧栏 → 闯关冲刺 | 按考点闯关，含选择题、填空题、简答题，答错自动重练 |
| **Athena AI** | 左侧栏 → Athena | 自由提问、论文代写、知识总结、复习计划等 |
| **错题本** | 左侧栏 → 错题本 | 自动收录错题，按课程分组，可查看解析 |
| **教师工作台** | 设置 → 我是教师 | AI 出卷、多语言翻译、导出 PDF |
| **本地账号** | 设置 → 账号信息 | 离线账号（昵称、emoji 头像、签名），支持导出/导入 |
| **数据管理** | 设置 → 数据管理 | 查看存储占用、按课程大小拆分、一键清除数据 |
| **检查更新** | 设置 → 检查更新 | 检查 GitHub Releases 新版本，一键跳转下载 |
| **主题切换** | 设置页 | 浅色 / 深色 / Vista / Win95 / Codex |
| **语言切换** | 设置页 | 简体中文 / English / Русский / 日本語 / 한국어 |

---

## 数据存储

所有数据存储在 **浏览器本地**（并非服务器）：

| 数据 | 存储位置 |
|------|---------|
| 课程、关卡、学习进度 | localStorage (`chillpass-course-v2`) |
| 本地账号信息 | localStorage (`chillpass-auth`) |
| 聊天记录（Athena） | localStorage (`chillpass-chat`) |
| Athena 记忆与能力 | localStorage (`athena-storage`) |
| API 配置 | localStorage (`chillpass-settings`) |
| 主题 | localStorage (`chillpass-theme`) |
| 语言 | localStorage (`chillpass-language`) |
| 学习时长 | localStorage (`chillpass-study-time`) |
| 错题本 | localStorage (`chillpass-wrong-questions`) |
| 上传的课件文件 | IndexedDB (`chillpass-files`) |

> **清除数据**：设置 → 数据管理 → 清除所有课程数据。  
> 清除后所有本地数据不可恢复。  
> **账号迁移**：设置 → 导出账号，得到 JSON 文件，在新设备导入即可。

---

## 配置 DeepSeek API

使用 AI 功能（考点提取、题目生成、AI 评阅、Athena 对话）需要配置 API Key：

1. 进入 **设置 → API 配置**
2. 填入你的 DeepSeek API Key（[deepseek.com](https://platform.deepseek.com/api_keys)）
3. 可选：调整模型（默认 `deepseek-chat`）、温度等参数

---

## 常见问题

**Q: 安装程序提示端口被占用？**  
A: 说明已有 ChillPass 实例在运行。安装时会自动关闭旧实例，稍等几秒后重试即可。

**Q: 双击 chillpass.exe 没反应？**  
A: 请确保 `dist/` 文件夹与 `chillpass.exe` 在同一目录。首次启动可能需要等待几秒。若仍无反应，可在命令行运行 exe 查看错误信息。

**Q: 启动后又打开了一个新页面？**  
A: 这是正常行为。检测到端口已被占用时，它会自动把浏览器聚焦到已有页面。

**Q: PDF 导出公式显示空白？**  
A: 确保导出时网络正常（KaTeX 字体需要从页面加载）。如果仍空白，请使用 Chromium 内核浏览器（Edge/Chrome）。

**Q: 数据会丢失吗？**  
A: 浏览器清除缓存时可能删除本地数据。建议定期在设置中导出账号信息备份。

**Q: 如何更新到新版本？**  
A: 两种方式：① 设置 → 检查更新 → 下载最新安装包覆盖安装；② 开始菜单 → ChillPass → 检查更新（带界面的更新工具，支持自动下载安装）。

**Q: 杀软报毒？**  
A: chillpass.exe 是使用 Node.js SEA 技术打包的独立应用，并非病毒。未做数字签名时部分杀软会有启发式警告，信任即可。

---

## 技术栈

- **前端**: React 18 + TypeScript + Vite 5 + React Router 6
- **状态管理**: Zustand（localStorage 持久化）
- **AI 引擎**: DeepSeek API
- **桌面壳**: Node.js SEA（单可执行应用，内置 HTTP 服务器）
- **安装程序**: NSIS 3（免管理员 per-user 安装）
- **系统托盘**: PowerShell + WinForms NotifyIcon
- **自动更新**: PowerShell 脚本（GitHub Releases 集成，支持代理回退）
- **样式**: CSS Modules + Framer Motion 动画 + SVG 液态玻璃滤镜
- **文件解析**: PDF.js / JSZip / Tesseract.js
- **文件存储**: IndexedDB
- **公式渲染**: KaTeX

---

## 项目结构

```
ChillPass-Web/
├── chillpass.exe              # 独立 exe 启动器（构建产物，已 gitignore）
├── dist/                      # 前端构建产物（已 gitignore）
├── installer/                 # 安装程序相关
│   ├── app.cjs                # SEA 入口（HTTP 服务器 + 托盘启动）
│   ├── build-sea.mjs          # SEA 打包脚本（生成 chillpass.exe）
│   ├── installer.nsi          # NSIS 安装程序脚本
│   ├── updater.ps1            # 自动更新检查器（静默 + GUI 模式）
│   └── build-installer.ps1    # 一键构建脚本
├── tray.ps1                   # 系统托盘脚本
├── src/                       # 源码目录
│   ├── components/            # 公共组件（含账号登录/编辑弹窗）
│   ├── pages/                 # 页面组件
│   ├── services/              # 核心服务（DeepSeek API、文件解析等）
│   ├── stores/                # Zustand 状态管理
│   ├── types/                 # TypeScript 类型定义
│   ├── utils/                 # 工具函数
│   └── styles/                # 全局样式与主题
├── server.mjs                 # 开发服务器（零依赖）
├── vite.config.ts             # Vite 配置
├── package.json               # 依赖管理
└── 使用说明.md                 # 本文件
```
